services:
  app:
    image: backend
    container_name: backend-dev`
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - UV_PROJECT_ENVIRONMENT=/venv
      - PATH=/venv/bin:$PATH
    stdin_open: true
    tty: true
    command: bash
